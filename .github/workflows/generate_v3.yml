name: Generate-v3

on:
  schedule:
    - cron: '0 0 1 * *' # 毎月1日の00:00に実行
  workflow_dispatch:
    inputs:
      generate:
        description: 'Run timetable generation'
        required: false
        default: 'true'
      create_pr:
        description: 'Create pull request'
        required: false
        default: 'true'

jobs:
  execute:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5.4.0
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Generate timetable(v3)
        if: ${{ github.event_name == 'schedule' || inputs.generate == 'true' }}
        run: |
          python script/v3/main.py

      - name: Check timetable(v3)
        run: |
          python script/v3/check_timetable.py --mode directory --path ./database/kanachu/v3/database --ignore busstops.json,route_ids.json,route.json

      - name: Create pull request
        if: ${{ github.event_name == 'schedule' || inputs.create_pr == 'true' }}
        uses: peter-evans/create-pull-request@v7
        with:
          commit-message: "Update timetable v3"
          title: "Update timetable v3"
          body: "Update timetable v3"
          branch: "feature/update-timetable-v3"
          delete-branch: true
